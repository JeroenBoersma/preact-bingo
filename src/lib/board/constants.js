
export const MAXFIELDS = 75;
export const NUMBERS = new Array(MAXFIELDS).fill(0).map((v, i) => i+1);
export const EMTPYFIELD = '';

export const COLUMNS = 5;
export const ROWS = 5;
export const EXCLUDE = {2: {2: true}};
